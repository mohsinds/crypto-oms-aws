apiVersion: v1
kind: Service
metadata:
  name: order-processor
  namespace: crypto-oms
  labels:
    app: order-processor
spec:
  type: ClusterIP
  # Note: Order Processor is a background service with no HTTP endpoints
  # This service is created for service discovery and potential future use
  ports: []
  selector:
    app: order-processor
